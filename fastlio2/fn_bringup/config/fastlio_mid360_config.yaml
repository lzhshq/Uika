/**:
    ros__parameters:
        # https://github.com/hku-mars/FAST_LIO/issues/33:
        feature_extract_enable: false
        point_filter_num: 3 # FineNav original MID360 front-end filter strength.
        max_iteration: 3 # the maximum iterations on ICP adjustment per scan, the influence is very tiny, I actually dont recommend to set it to large.
        filter_size_surf: 0.05 # FineNav original bringup value.
        filter_size_map: 0.05 # FineNav original bringup value.
        cube_side_length: 1000.0
        runtime_pos_log_enable: false
        map_file_path: "/home/fins/Desktop/pcd/test.pcd"

        common:
            lid_topic:  "/lidar"
            imu_topic:  "/imu"
            initial_frame: "lidar_odom"
            body_frame: "base_lidar"
            time_sync_en: false         # Trust Livox driver stamps; do not run FAST-LIO software LiDAR/IMU self-sync.
            time_offset_lidar_to_imu: 0.0 # Time offset between lidar and IMU calibrated by other algorithms, e.g. LI-Init (can be found in README).
                                        # This param will take effect no matter what time_sync_en is. So if the time offset is not known exactly, please set as 0.0
            use_imu_odometry: false            # false = 输出 LiDAR 校正后的里程计；true 的 IMU 积分会在静止时漂移
            imu_window_size: 20        # imu 滑动窗口大小，沿用FAST-LIO2 MID360配置

        preprocess:
            lidar_type: 1                # 1 for Livox serials LiDAR, 2 for Velodyne LiDAR, 3 for ouster LiDAR, 4 for any other pointcloud input
            scan_line:  4               # resolution of your lidar, for ex; scan_line for vlp-16 is 16
            blind: 0.5                  # Unit:m, blind is the region removed from the processed pointcloud, this is required to remove the robot body, in other words, all the lidar points inside the blind are removed
            timestamp_unit: 3
            scan_rate: 20

        mapping:
            acc_cov: 0.1
            gyr_cov: 0.1
            b_acc_cov: 0.0001
            b_gyr_cov: 0.0001
            fov_degree:    360.0
            det_range:     100.0
            extrinsic_est_en:  false     # false: use fixed IMU-LiDAR extrinsic
            extrinsic_T: [ -0.011, -0.02329, 0.04412 ]
            extrinsic_R: [ 1., 0., 0.,
                            0., 1., 0.,
                            0., 0., 1.]



        publish:
            path_en: true                # true: publish Path
            effect_map_en: false         # true: publish Effects
            map_en: false                # false: do not publish accumulated Map cloud; /map_save still writes the internal map buffer
            scan_publish_en:  false      # false: close all live point cloud output
            dense_publish_en: false      # Publish downsampled scan; RViz uses decay/map display for visual density.
            scan_bodyframe_pub_en: false # true: output the point cloud scans in IMU-body-frame
            tf_en: false                  # TF由localization_manager根据/Odometry发布，避免重复TF源

        pcd_save:
            pcd_save_en: true
            interval: -1                 # how many LiDAR frames saved in each pcd file; 
                                        # -1 : all frames will be saved in ONE pcd file, may lead to memory crash when having too much frames.
